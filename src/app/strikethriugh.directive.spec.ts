import { StrikethriughDirective } from './strikethriugh.directive';

describe('StrikethriughDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikethriughDirective();
    expect(directive).toBeTruthy();
  });
});
